# 工业摇杆产品线软件架构设计

## 一、问题分析

一个工业摇杆产品线通常面临三个维度的变化：

```
维度1：产品型号（硬件差异）
├── JS-100：单轴摇杆，2按键，模拟输出
├── JS-200：双轴摇杆，4按键，CAN输出
├── JS-300：三轴摇杆+旋钮，8按键，CAN+安全功能
└── JS-400：双轴+力反馈，12按键，EtherCAT

维度2：通信协议（客户需求差异）
├── 模拟量输出（0-5V / 4-20mA）
├── PWM 输出
├── CAN 2.0B（自定义协议）
├── CANopen（DS401 / DS406）
├── SAE J1939
├── CANopen Safety（EN 50325-5）
└── EtherCAT / PROFINET（未来扩展）

维度3：运行平台（芯片差异）
├── STM32F103
├── STM32G431
├── S32K144
└── 未来平台
```

如果用排列组合，4×7×3 = 56 种配置。传统做法是每种配置一个工程，维护噩梦。
正确的做法是用**组合而非继承**，通过架构设计让这三个维度正交独立。

---

## 二、总体架构

```
┌────────────────────────────────────────────────────────────────┐
│                       Configuration Layer                       │
│  config/products/js*.c（每产品一文件）/ build system (CMake)      │
├────────────────────────────────────────────────────────────────┤
│                        Event Bus (观察者)                        │
│  事件发布/订阅总线 — 解耦模块间通信（支持 ISR 延迟分发）           │
├────────────────────────────────────────────────────────────────┤
│              RTOS Task Layer (任务调度)                          │
│  safety_task(最高) / main_task / proto_task / diag_task(最低)   │
├────────────────────────────────────────────────────────────────┤
│                       Application Layer                         │
│  device_state(状态机) / app_compose(组装) / app_tasks(任务入口)  │
├──────────────┬─────────────────────────────────────────────────┤
│  Joystick    │  Safety Monitor  │  Diagnostics (STL/WDG)       │
│  信号处理链   │  安全监控(高优先级)│  诊断服务(低优先级)           │
├──────────────┴─────────────────┴───────────────────────────────┤
│                        Service Layer                            │
│  protocol_manager / calibration / nv_storage / logging         │
├──────────┬─────────────────────────────────────────────────────┤
│ Protocol │  CANopen │ J1939 │ Analog │ CAN_Raw │ ...           │
│ Plugins  │  (DS401) │       │ Output │         │               │
│ (能力模型)│         │       │        │         │               │
├──────────┴─────────────────────────────────────────────────────┤
│              Signal Processing Stages (信号处理管线)              │
│  fault_detector / calibrator / filter / deadzone / debounce    │
├────────────────────────────────────────────────────────────────┤
│              Input Acquisition Layer (输入采集层)                 │
│  scan_buffer_t ← board_scanner_t / buf_axis_source / buf_btn   │
├────────────────────────────────────────────────────────────────┤
│                    Driver Interface Layer                        │
│  adc_reader.h / adc_voltage_reader.h / adc_continuous.h        │
│  can_driver.h / gpio_reader.h / dac_driver.h / spi_driver.h   │
├────────────────────────────────────────────────────────────────┤
│              RTOS Abstraction Layer (os_abstraction.h)          │
│  os_task / os_mutex / os_queue / os_delay                      │
├────────────────────────────────────────────────────────────────┤
│                    Platform Registry                            │
│  platform_registry.h — platform_init() 显式注册（MISRA 合规）    │
├──────────┬──────────┬──────────────────────────────────────────┤
│ STM32G4  │ S32K144  │  PC Mock                                 │
│ Platform │ Platform │  Platform                                │
├──────────┴──────────┴──────────────────────────────────────────┤
│                    BSP (Board Support)                           │
│  引脚映射 / 时钟配置 / 中断优先级 / CubeMX生成代码               │
└────────────────────────────────────────────────────────────────┘
```

### 架构演进历程

| 变化点 | v1 设计 | v2 改进 | v3 改进 | 对应原则 |
|--------|---------|---------|---------|----------|
| 摇杆核心 | 单函数五职责 | 信号处理链 pipeline | 同 v2 | SRP |
| 输入采集 | 直接调 ADC | 同 v1 | `scan_buffer_t` + `board_scanner_t` 扫描-缓冲解耦 | SRP+OCP |
| 按键采集 | 仅 GPIO | 同 v1 | `buf_btn_source`（MUX 通道阈值检测，支持多键同按） | ISP |
| 产品配置 | `#elif` 链 | 每产品独立 `.c` | 同 v2（满量程由 `resolution_bits` 推导，无额外配置） | OCP |
| 协议接口 | 7方法全实现 | 能力位掩码 | 同 v2 | LSP |
| ADC 接口 | 胖接口 | ISP 拆分 | 新增 SPI 接口 | ISP |
| 平台注册 | `#if defined` | `__attribute__((constructor))` | 显式 `platform_init()`（MISRA 合规） | DIP |
| 归一化范围 | ±10000 固定 | 同 v1 | 内部无符号原始范围，协议层按需转换 | — |
| 任务架构 | 裸循环 | 裸循环 + `os_delay` | RTOS 多任务（优先级隔离） | SRP |
| 事件总线 | 无 | 同步分发 | 增加 ISR 安全延迟分发 | 观察者 |
| 死区策略 | 硬编码线性死区 | 可插拔策略（线性/S曲线/指数） | 新增 |

---

## 三、驱动接口层设计（ISP 改进：按使用场景拆分）

### 3.1 ADC 接口 — 按使用场景隔离

原设计将所有 ADC 功能放在一个 `adc_driver_t` 中。摇杆核心只需 `read()`，安全监控需要 `read_mv()`，DMA 采集才需要 `start_continuous()`。按接口隔离原则拆分为三个独立接口：

```c
// drivers/adc_reader.h — 基础读取接口，大多数模块只需要这个
#pragma once
#include "drv_common.h"

typedef struct {
    drv_status_t (*init)(void);
    drv_status_t (*read)(adc_channel_id_t ch, uint16_t *raw);
    drv_status_t (*deinit)(void);
} adc_reader_t;
```

```c
// drivers/adc_voltage_reader.h — 电压读取接口，安全监控/诊断需要
#pragma once
#include "drv_common.h"

typedef struct {
    drv_status_t (*read_mv)(adc_channel_id_t ch, uint32_t *mv);
} adc_voltage_reader_t;
```

```c
// drivers/adc_continuous.h — DMA 连续采集接口，高速采集模块需要
#pragma once
#include "drv_common.h"

typedef struct {
    drv_status_t (*start_continuous)(adc_channel_id_t *chs, uint8_t cnt,
                                     uint16_t *buf, uint16_t buf_len);
    drv_status_t (*stop_continuous)(void);
} adc_continuous_t;
```

```c
// drivers/drv_common.h — 公共类型定义
#pragma once
#include <stdint.h>

typedef enum {
    DRV_OK = 0,
    DRV_ERROR,
    DRV_TIMEOUT,
    DRV_BUSY,
} drv_status_t;

typedef enum {
    ADC_CH_AXIS_X,
    ADC_CH_AXIS_Y,
    ADC_CH_AXIS_Z,
    ADC_CH_KNOB,
    ADC_CH_VREF,
    ADC_CH_TEMP,
    ADC_CH_SUPPLY,
    ADC_CH_MAX
} adc_channel_id_t;
```

平台实现可以同时满足多个接口：

```c
// platform/stm32g4/adc_stm32g4.c
const adc_reader_t          adc_reader_stm32g4 = { .init = ..., .read = ..., .deinit = ... };
const adc_voltage_reader_t  adc_voltage_stm32g4 = { .read_mv = ... };
const adc_continuous_t      adc_continuous_stm32g4 = { .start_continuous = ..., .stop_continuous = ... };
```

各模块只依赖自己需要的最小接口：

```c
// 摇杆核心只依赖基础读取
void joystick_core_init(joystick_core_t *js,
                         const adc_reader_t *adc,
                         const gpio_reader_t *gpio);

// 安全监控依赖电压读取接口
void safety_monitor_init(safety_monitor_t *sm,
                          const adc_voltage_reader_t *adc,
                          const gpio_reader_t *gpio);
```

### 3.2 CAN 驱动接口

```c
// drivers/drv_can.h
#pragma once
#include "drv_common.h"

typedef struct {
    uint32_t id;
    uint8_t  data[64];   // 支持 CAN-FD
    uint8_t  dlc;
    uint8_t  is_extended; // 0=标准帧 1=扩展帧
    uint8_t  is_fd;       // 0=CAN2.0 1=CAN-FD
} can_frame_t;

typedef void (*can_rx_callback_t)(const can_frame_t *frame);

typedef struct {
    drv_status_t (*init)(uint32_t baudrate);
    drv_status_t (*send)(const can_frame_t *frame);
    drv_status_t (*set_filter)(uint32_t id, uint32_t mask);
    drv_status_t (*register_rx_cb)(can_rx_callback_t cb);
    drv_status_t (*get_bus_state)(uint8_t *tx_err, uint8_t *rx_err);
    drv_status_t (*deinit)(void);
} can_driver_t;
```

### 3.3 GPIO 驱动接口

```c
// drivers/drv_gpio.h
typedef enum {
    GPIO_PIN_BTN_1, GPIO_PIN_BTN_2, GPIO_PIN_BTN_3, GPIO_PIN_BTN_4,
    GPIO_PIN_BTN_5, GPIO_PIN_BTN_6, GPIO_PIN_BTN_7, GPIO_PIN_BTN_8,
    GPIO_PIN_LED_STATUS, GPIO_PIN_LED_ERROR,
    GPIO_PIN_ENABLE_OUT,
    GPIO_PIN_MAX
} gpio_pin_id_t;

// 按 ISP 拆分：读取和写入分离
typedef struct {
    drv_status_t (*init)(void);
    drv_status_t (*read)(gpio_pin_id_t pin, uint8_t *state);
} gpio_reader_t;

typedef struct {
    drv_status_t (*write)(gpio_pin_id_t pin, uint8_t state);
    drv_status_t (*toggle)(gpio_pin_id_t pin);
} gpio_writer_t;
```

### 3.4 DAC 驱动接口

```c
// drivers/drv_dac.h（用于模拟输出型号）
typedef enum {
    DAC_CH_AXIS_X_OUT,
    DAC_CH_AXIS_Y_OUT,
    DAC_CH_MAX
} dac_channel_id_t;

typedef struct {
    drv_status_t (*init)(void);
    drv_status_t (*write_mv)(dac_channel_id_t ch, uint32_t millivolts);
    drv_status_t (*deinit)(void);
} dac_driver_t;
```

### 3.5 平台注册机制（DIP 改进：依赖方向完全反转）

原设计中 `app_config.c` 通过 `#if defined(PLATFORM_STM32G4)` 直接引用平台符号，编译时依赖方向从上往下。改进后，平台层主动向上注册，上层代码完全不知道具体平台的存在。

```
改进前（依赖方向向下）：
  app_config.c ──→ platform/stm32g4/adc_stm32g4.c

改进后（依赖方向向上）：
  app_config.c ──→ drivers/adc_reader.h ←── platform/stm32g4/adc_stm32g4.c
```

```c
// platform/platform_registry.h — 上层只依赖这个抽象接口
#pragma once
#include "adc_reader.h"
#include "adc_voltage_reader.h"
#include "drv_can.h"
#include "drv_gpio.h"
#include "drv_dac.h"
#include "drv_spi.h"

void platform_register_adc(const adc_reader_t *drv);
void platform_register_adc_voltage(const adc_voltage_reader_t *drv);
void platform_register_can(const can_driver_t *drv);
void platform_register_gpio_reader(const gpio_reader_t *drv);
void platform_register_gpio_writer(const gpio_writer_t *drv);
void platform_register_dac(const dac_driver_t *drv);
void platform_register_spi(const spi_driver_t *drv);

const adc_reader_t*          platform_get_adc(void);
const adc_voltage_reader_t*  platform_get_adc_voltage(void);
const can_driver_t*          platform_get_can(void);
const gpio_reader_t*         platform_get_gpio_reader(void);
const gpio_writer_t*         platform_get_gpio_writer(void);
const dac_driver_t*          platform_get_dac(void);
const spi_driver_t*          platform_get_spi(void);

// 显式初始化入口 — 每个平台实现一个
// 替代 __attribute__((constructor))，满足 MISRA C Rule 1.2
void platform_init(void);
```

```c
// platform/stm32g4/platform_init.c
// 显式调用，初始化顺序确定性可控，可设断点调试
// 不依赖任何 GCC 扩展，满足功能安全要求

#include "platform_registry.h"
#include "adc_stm32g4.h"
#include "can_stm32g4.h"
#include "gpio_stm32g4.h"
#include "spi_stm32g4.h"

void platform_init(void) {
    platform_register_adc(&adc_reader_stm32g4);
    platform_register_adc_voltage(&adc_voltage_stm32g4);
    platform_register_can(&can_driver_stm32g4);
    platform_register_gpio_reader(&gpio_reader_stm32g4);
    platform_register_gpio_writer(&gpio_writer_stm32g4);
    platform_register_spi(&spi_driver_stm32g4);
    // DAC 仅在带模拟输出的产品上注册
    #if defined(HAS_DAC)
    platform_register_dac(&dac_driver_stm32g4);
    #endif
}
```

> **为什么不用 `__attribute__((constructor))`？**
>
> | 对比项 | `__attribute__((constructor))` | 显式 `platform_init()` |
> |--------|-------------------------------|----------------------|
> | MISRA C 合规 | 违反 Rule 1.2（实现定义行为） | 合规 |
> | 初始化顺序 | 不确定，跨编译单元无法保证 | 代码中显式控制 |
> | 可调试性 | 在 `main()` 之前执行，断点难设 | 普通函数调用，随时断点 |
> | 功能安全认证 | IEC 61508 / ISO 13849 审核不通过 | 可审核、可追溯 |
> | 可移植性 | GCC 专属，IAR/ARMCC/Green Hills 不支持 | 纯 C 标准，所有编译器通用 |

```c
// main.c — 启动顺序清晰
int main(void) {
    // 1. 硬件初始化（BSP / CubeMX 生成）
    HAL_Init();
    SystemClock_Config();

    // 2. 平台驱动注册（显式调用，替代 constructor）
    platform_init();

    // 3. RTOS 内核初始化
    os_kernel_init();

    // 4. 创建应用任务
    app_tasks_create();

    // 5. 启动调度器（不返回）
    os_kernel_start();
}
```

链接器决定哪个平台的 `platform_init.c` 参与编译，上层代码零修改。每个平台只需提供自己的 `platform_init()` 实现即可。

### 3.6 SPI 驱动接口（霍尔角度传感器等）

```c
// drivers/drv_spi.h — 用于霍尔角度传感器、外部 ADC 等
#pragma once
#include "drv_common.h"

typedef struct {
    drv_status_t (*init)(uint32_t clock_hz);
    drv_status_t (*transfer)(uint8_t cs_pin,
                              const uint8_t *tx, uint8_t *rx,
                              uint16_t len);
    drv_status_t (*deinit)(void);
} spi_driver_t;
```

---

## 四、输入采集层（scan_buffer 扫描-缓冲解耦模式）

### 4.1 设计动机

原设计假设"轴 = ADC 通道"，`joystick_update()` 直接调用 `adc->read(ch, &raw)`。但实际工业摇杆的输入源远比这复杂：

```
实际硬件拓扑（以 JS-300 为例）：
         5V ──┬── R1 ──┬── GND     5V ──┬── R1 ──┬── GND
              │ Button │                │ Button │
              2.5V     │                2.5V     │
                ↓      ↓                  ↓      ↓
        ┌──────────────────────────────────────────┐
        │       Analog MUX (CD74HC4067)            │
        │  CH0: Axis-X pot    CH4: Button-0 (2.5V) │
        │  CH1: Axis-Y pot    CH5: Button-1 (2.5V) │
        │  CH2: Axis-Z pot    CH6: Button-2 (2.5V) │
        │  CH3: (reserved)    CH7: Button-3 (2.5V) │
        └──────────────┬───────────────────────────┘
                       │ COM (共用输出)
                       ↓
                  ┌─────────┐
                  │   ADC   │ ← 同一个 ADC 通道
                  └─────────┘
```

关键约束：
1. **轴和按键共用 MUX+ADC 资源**——不能各自独立读取，必须协调扫描顺序
2. **按键采用逐通道 5V→2.5V 分压**——每个按键占独立 MUX 通道，按下时读 ≥2.5V，支持多键同按
3. **不同产品硬件差异大**——有的用 MUX，有的直接 ADC 多通道，有的用 DMA 连续采集

若将 MUX+ADC 作为整体资源封装进输入源，会导致轴源和按键源耦合在同一个扫描器上，缺乏平台化能力。因此需要一个**纯数据解耦点**。

### 4.2 scan_buffer 解耦模式

核心思想：将"谁扫描硬件"和"谁消费数据"通过纯数据结构 `scan_buffer_t` 解耦。

```
                    ┌──────────────────────────────┐
                    │    Board-level Scanner        │
                    │    (board_scanner_t)          │
                    │  ┌──────────┐ ┌───────────┐  │
                    │  │mux_adc   │ │direct_adc │  │   可替换策略
                    │  │_scan     │ │_scan      │  │
                    │  └────┬─────┘ └─────┬─────┘  │
                    │       └──────┬──────┘        │
                    └──────────────┼───────────────┘
                                   │ scanner.scan(ctx, &buf)
                                   ▼
                    ┌──────────────────────────────┐
                    │    scan_buffer_t              │  ← 纯数据，零依赖
                    │    uint16_t values[16]        │
                    │    uint8_t  count             │
                    └──────────┬───────────────────┘
                               │ 只读引用
                    ┌──────────┴───────────┐
                    ↓                      ↓
          ┌──────────────┐       ┌──────────────┐
          │buf_axis_source│       │buf_btn_source │
          │ .read_raw()  │       │ .read_buttons()│
          │ 按 channel   │       │ 阈值检测       │
          │ 索引取值      │       │ 多键同按       │
          └──────┬───────┘       └──────┬────────┘
                 ↓                      ↓
          Signal Chain             Debounce
```

### 4.3 scan_buffer — 纯数据缓冲区

```c
// input/scan_buffer.h — 纯数据结构，零外部依赖
#pragma once
#include <stdint.h>

#define SCAN_BUF_MAX_CHANNELS  16

typedef struct {
    uint16_t values[SCAN_BUF_MAX_CHANNELS];  // 各通道原始 ADC 值
    uint8_t  count;                           // 有效通道数
} scan_buffer_t;
```

`scan_buffer_t` 是整个输入采集层的**解耦核心**：
- 上游（扫描器）只写入它
- 下游（输入源）只读取它
- 它本身不依赖任何硬件接口，可在 PC Mock 上直接填充测试数据

### 4.4 board_scanner — 板级扫描策略

```c
// board/board_scan.h — 扫描策略接口（板级关注点）
#pragma once
#include "scan_buffer.h"

typedef struct {
    void (*scan)(void *ctx, scan_buffer_t *buf);  // 一次扫描所有通道
    void *ctx;                                     // 指向具体策略的上下文
} board_scanner_t;
```

不同产品/硬件使用不同的扫描策略：

**MUX + ADC 扫描策略**（JS-300/JS-400 等使用 MUX 的产品）：

```c
// board/mux_adc_scan.c — MUX 选通 + ADC 逐通道采集
#include "board_scan.h"
#include "adc_reader.h"
#include "drv_gpio.h"

typedef struct {
    const adc_reader_t  *adc;          // ADC 驱动接口
    const gpio_writer_t *gpio;         // MUX 选通引脚控制
    adc_channel_id_t     adc_channel;  // MUX COM 对应的 ADC 通道
    gpio_pin_id_t        sel_pins[4];  // MUX 地址选择引脚 (S0-S3)
    uint8_t              sel_pin_count; // 实际使用的选择引脚数
    uint8_t              total_channels;// 需要扫描的总通道数
    uint16_t             settle_us;    // MUX 切换后建立时间 (μs)
} mux_adc_scan_ctx_t;

static void mux_adc_scan(void *ctx, scan_buffer_t *buf) {
    mux_adc_scan_ctx_t *c = (mux_adc_scan_ctx_t *)ctx;
    buf->count = c->total_channels;

    for (uint8_t ch = 0; ch < c->total_channels; ch++) {
        // 设置 MUX 地址选择引脚
        for (uint8_t s = 0; s < c->sel_pin_count; s++) {
            const gpio_level_t level = (ch >> s) & 1u ? GPIO_HIGH : GPIO_LOW;
            c->gpio->write(c->sel_pins[s], level);
        }
        // 等待 MUX 输出建立
        drv_delay_us(c->settle_us);
        // 读取 ADC
        uint16_t raw = 0;
        c->adc->read(c->adc_channel, &raw);
        buf->values[ch] = raw;
    }
}
```

**直接 ADC 多通道扫描策略**

```c
// board/direct_adc_scan.c — 多个独立 ADC 通道直接读取
#include "board_scan.h"
#include "adc_reader.h"

typedef struct {
    const adc_reader_t *adc;
    adc_channel_id_t    channels[SCAN_BUF_MAX_CHANNELS];
    uint8_t             channel_count;
} direct_adc_scan_ctx_t;

static void direct_adc_scan(void *ctx, scan_buffer_t *buf) {
    direct_adc_scan_ctx_t *c = (direct_adc_scan_ctx_t *)ctx;
    buf->count = c->channel_count;

    for (uint8_t i = 0; i < c->channel_count; i++) {
        uint16_t raw = 0;
        c->adc->read(c->channels[i], &raw);
        buf->values[i] = raw;
    }
}
```

**DMA 连续采集策略**

```c
// board/dma_scan.c — DMA 硬件自动连续采集
#include "board_scan.h"
#include "adc_continuous.h"

typedef struct {
    const adc_continuous_t *adc_cont;
    uint16_t               *dma_buf;    // DMA 目标缓冲区
    uint8_t                 channel_count;
} dma_scan_ctx_t;

static void dma_scan(void *ctx, scan_buffer_t *buf) {
    dma_scan_ctx_t *c = (dma_scan_ctx_t *)ctx;
    buf->count = c->channel_count;

    // DMA 缓冲区由硬件自动更新，直接拷贝最新值
    for (uint8_t i = 0; i < c->channel_count; i++) {
        buf->values[i] = c->dma_buf[i];
    }
}
```

### 4.5 buf_axis_source — 缓冲区轴输入源

```c
// input/buf_axis_source.h
#pragma once
#include "drv_common.h"
#include "scan_buffer.h"

// 轴输入源统一接口（信号处理链依赖此接口）
typedef struct {
    drv_status_t (*read_raw)(void *ctx, int32_t *raw_value);
    void *ctx;
    uint8_t resolution_bits;    // 12 for 12-bit ADC, 14 for AS5048A, etc.
} axis_source_t;

// 基于 scan_buffer 的轴输入源上下文
typedef struct {
    const scan_buffer_t *buf;         // 只读引用，不拥有
    uint8_t              channel_index; // 该轴在 scan_buffer 中的通道索引
} buf_axis_ctx_t;

static inline drv_status_t buf_axis_read(void *ctx, int32_t *raw) {
    const buf_axis_ctx_t *c = (const buf_axis_ctx_t *)ctx;
    if (c->channel_index >= c->buf->count) {
        return DRV_ERR_PARAM;
    }
    *raw = (int32_t)c->buf->values[c->channel_index];
    return DRV_OK;
}
```

### 4.6 buf_btn_source — 缓冲区按键输入源

```c
// input/buf_btn_source.h
#pragma once
#include "drv_common.h"
#include "scan_buffer.h"

// 按键输入源统一接口
typedef struct {
    drv_status_t (*read_buttons)(void *ctx, uint16_t *button_mask);
    void *ctx;
    uint8_t button_count;
} button_source_t;

// 基于 scan_buffer 的按键输入源上下文
// 每个按键占独立 MUX 通道，5V→2.5V 分压，按下时读数 ≥ threshold
typedef struct {
    const scan_buffer_t *buf;
    uint8_t    channel_indices[12];  // 各按键对应的 scan_buffer 通道索引
    uint16_t   threshold;            // 判定为按下的 ADC 阈值（对应 ~2.5V）
    uint8_t    count;                // 按键数量
} buf_btn_ctx_t;

static inline drv_status_t buf_btn_read(void *ctx, uint16_t *mask) {
    const buf_btn_ctx_t *c = (const buf_btn_ctx_t *)ctx;
    *mask = 0;
    for (uint8_t i = 0; i < c->count; i++) {
        if (c->buf->values[c->channel_indices[i]] >= c->threshold) {
            *mask |= (1u << i);
        }
    }
    return DRV_OK;
}
```

阈值检测方式天然支持**多键同按**：每个按键在独立 MUX 通道上有独立的分压电路，互不干扰。

### 4.7 产品配置中组装输入源

产品描述符中声明扫描策略和通道映射，在 `app_compose` 阶段根据配置构造对应实例。不同产品选择不同的 `board_scanner_t` 策略：

### 4.8 更新后的输入数据流

```
硬件传感器 ──→ ┌─────────────────────────────────────────────┐
               │         Board Scanner Layer                  │
               │  board_scanner_t.scan(ctx, &buf)             │
               │  (mux_adc_scan / direct_adc_scan / dma_scan) │
               └──────────────────┬──────────────────────────┘
                                  │ 写入
                                  ▼
               ┌─────────────────────────────────────────────┐
               │         scan_buffer_t (纯数据解耦点)          │
               │  values[0..N] — 所有通道的原始 ADC 值         │
               └─────────┬───────────────────┬───────────────┘
                         │ 只读               │ 只读
                         ↓                    ↓
               ┌──────────────────┐ ┌──────────────────────┐
               │  buf_axis_source │ │  buf_btn_source      │
               │  按 channel 取值 │ │  阈值检测 → bitmask  │
               └────────┬─────────┘ └──────────┬───────────┘
                        │ int32_t raw           │ uint16_t mask
                        ↓                       ↓
               ┌──────────────────┐    ┌──────────────┐
               │  Signal Chain    │    │  Debounce    │
               │  fault→cal→flt→dz│    │              │
               └────────┬─────────┘    └──────┬───────┘
                        │ int16_t              │ uint16_t
                        └──────────┬───────────┘
                                   ↓
               ┌──────────────────────────────────────────┐
               │      joystick_data_t                     │
               │  axes[] + buttons + safety_state         │
               └──────────────────────────────────────────┘
```

---

## 五、信号处理链架构（SRP 改进：拆分处理管线）

### 5.1 内部信号表示：无符号原始范围

信号处理链**不做归一化**，全程保持传感器原始无符号范围：

| 传感器 | 原始范围 | 中心值 | 数据类型 |
|--------|---------|--------|---------|
| 12-bit ADC | 0–4095 | ~2048（校准值） | uint16_t |
| 14-bit Hall | 0–16383 | ~8192（校准值） | uint16_t |

**设计原则：**

- **全程无符号** — ADC 电压、霍尔角度本身就是正值，引入有符号数增加不必要的符号处理
- **零精度损失** — 不做中间归一化映射，传感器原始分辨率完整保留
- **无配置项** — 满量程由 `resolution_bits` 直接推导，不需要额外的 `axis_full_scale` 字段
- **方向与幅值自然分离** — 相对于校准中心点的偏移量隐含方向信息，协议输出层按需转换

```c
// signal/axis_raw.h — 内部信号表示
#pragma once
#include <stdint.h>

#define AXIS_RANGE(res_bits) (1U << (res_bits))  // 满量程：4096 / 16384

typedef struct {
    uint16_t value;    // 无符号原始值（0–4095 或 0–16383）
    uint16_t center;   // 校准中心点
    uint16_t range;    // 满量程，由 AXIS_RANGE(resolution_bits) 推导
} axis_raw_t;
```

有符号转换**仅在协议输出层**执行一次：

```c
// protocol/usb_hid_adapter.c — 仅此处做有符号转换
int16_t axis_to_hid(axis_raw_t raw) {
    int32_t offset = (int32_t)raw.value - raw.center;
    int32_t half = (offset >= 0)
        ? (raw.range - raw.center)   // 正向量程（支持不对称校准）
        : raw.center;                // 负向量程
    return (int16_t)(offset * 32767 / half);
}
```

### 5.2 设计动机

原设计中 `joystick_core.c` 的 `process_axis()` 函数同时负责 ADC 采集、故障检测、校准计算、死区处理、按键消抖五个职责。任何一个需求变化（换消抖算法、换死区曲线、加数字滤波）都要修改这个文件。

改进：拆分为信号处理管线（pipeline），每个阶段是独立模块，各自只有一个变更理由。

### 5.3 信号处理链接口

```c
// signal/signal_chain.h — 信号处理链框架
#pragma once
#include <stdint.h>

#define SIGNAL_CHAIN_MAX_STAGES 8

// 每个处理阶段的统一接口
typedef struct {
    int16_t (*process)(int16_t input, void *ctx);
    void *ctx;
} signal_stage_t;

typedef struct {
    signal_stage_t stages[SIGNAL_CHAIN_MAX_STAGES];
    uint8_t count;
} signal_chain_t;

void    signal_chain_init(signal_chain_t *chain);
void    signal_chain_add(signal_chain_t *chain,
                          int16_t (*process)(int16_t, void *),
                          void *ctx);
int16_t signal_chain_execute(signal_chain_t *chain, int16_t input);
```

```c
// signal/signal_chain.c
int16_t signal_chain_execute(signal_chain_t *chain, int16_t input) {
    int16_t val = input;
    for (uint8_t i = 0; i < chain->count; i++) {
        val = chain->stages[i].process(val, chain->stages[i].ctx);
    }
    return val;
}
```

### 5.4 各阶段独立实现

每个阶段独立在自己的文件中，各自只有一个变更理由：

```c
// signal/fault_detector.c — 只管开路/短路检测
typedef struct {
    uint16_t threshold_low;   // 如 50
    uint16_t threshold_high;  // 如 4046
    uint8_t  fault_flag;      // 输出：是否故障
} fault_detect_ctx_t;

int16_t fault_detect_stage(int16_t input, void *ctx) {
    fault_detect_ctx_t *fc = (fault_detect_ctx_t *)ctx;
    uint16_t raw = (uint16_t)input;
    if (raw < fc->threshold_low || raw > fc->threshold_high) {
        fc->fault_flag = 1;
        return 0;  // 安全值
    }
    fc->fault_flag = 0;
    return input;
}
```

```c
// signal/calibrator.c — 只管线性校准
typedef struct {
    uint16_t min;
    uint16_t center;
    uint16_t max;
} calibrator_ctx_t;

int16_t calibrate_stage(int16_t input, void *ctx) {
    calibrator_ctx_t *cal = (calibrator_ctx_t *)ctx;
    uint16_t raw = (uint16_t)input;
    int32_t normalized;
    if (raw <= cal->center) {
        normalized = (int32_t)(raw - cal->center) * 10000
                     / (int32_t)(cal->center - cal->min);
    } else {
        normalized = (int32_t)(raw - cal->center) * 10000
                     / (int32_t)(cal->max - cal->center);
    }
    return (int16_t)normalized;
}
```

```c
// signal/filter.c — 只管数字滤波（原设计缺失）
typedef struct {
    int32_t prev_output;
    uint8_t alpha;  // 0-255，越大越平滑
} lowpass_ctx_t;

int16_t filter_stage(int16_t input, void *ctx) {
    lowpass_ctx_t *lp = (lowpass_ctx_t *)ctx;
    int32_t out = (lp->alpha * lp->prev_output
                   + (256 - lp->alpha) * (int32_t)input) / 256;
    lp->prev_output = out;
    return (int16_t)out;
}
```

```c
// signal/deadzone.c — 只管死区曲线，支持策略注入
typedef int16_t (*deadzone_fn_t)(int16_t input, int16_t threshold);

// 内置策略
int16_t deadzone_linear(int16_t input, int16_t threshold);
int16_t deadzone_scurve(int16_t input, int16_t threshold);
int16_t deadzone_exponential(int16_t input, int16_t threshold);

typedef struct {
    int16_t threshold;
    deadzone_fn_t fn;  // 策略函数指针
} deadzone_ctx_t;

int16_t deadzone_stage(int16_t input, void *ctx) {
    deadzone_ctx_t *dz = (deadzone_ctx_t *)ctx;
    return dz->fn(input, dz->threshold);
}
```

```c
// signal/debounce.c — 只管按键消抖
// 独立于轴信号处理链，用于按键处理
typedef struct {
    uint8_t  history[12];  // 每个按键的历史采样
    uint8_t  count;
    uint16_t debounced;
} debounce_ctx_t;

void debounce_update(debounce_ctx_t *ctx, uint8_t btn_idx, uint8_t state);
uint16_t debounce_get_buttons(const debounce_ctx_t *ctx);
```

### 5.5 显式信号管线（替代泛型 pipeline）

管线阶段顺序在产品中基本固定，真正变化的是每个阶段的**参数和策略**。用显式函数代替泛型 pipeline，类型分界清晰：

```c
// signal/axis_pipeline.h
#pragma once
#include <stdint.h>
#include "product_descriptor.h"

typedef struct {
    // 故障检测上下文
    fault_detect_ctx_t fault_ctx;
    // 校准上下文
    calibrator_ctx_t   cal_ctx;
    calibrate_fn_t     calibrate;     // 线性 / 环形（产品配置注入）
    // 滤波上下文
    lowpass_ctx_t      filter_ctx;
    // 死区
    int32_t            dz_threshold;
    deadzone_fn_t      dz_fn;
    // 输出
    int32_t            output;
} axis_pipeline_t;

void axis_pipeline_init(axis_pipeline_t *p,
                        const axis_pipeline_config_t *cfg,
                        calibrate_fn_t calibrate);
void axis_pipeline_process(axis_pipeline_t *p, uint16_t raw);
```

```c
// signal/axis_pipeline.c
void axis_pipeline_process(axis_pipeline_t *p, uint16_t raw) {
    // 1. 故障检测（uint16_t 域）
    if (fault_detect(&p->fault_ctx, raw)) {
        p->output = 0;
        return;
    }

    // 2. 校准（uint16_t → int32_t，类型分界点）
    //    线性传感器: (int32_t)raw - center
    //    数字角度:   circular_subtract(raw, center)
    int32_t val = p->calibrate(raw, &p->cal_ctx);

    // 3. 低通滤波（int32_t 域）
    if (p->filter_ctx.alpha > 0) {
        val = lowpass_filter(&p->filter_ctx, val);
    }

    // 4. 死区（int32_t 域，围绕零点）
    val = p->dz_fn(val, p->dz_threshold);

    p->output = val;
}
```

```c
// joystick 初始化与执行
void joystick_init(joystick_t *js, const product_descriptor_t *product) {
    for (uint8_t i = 0; i < product->axis_map_count; i++) {
        axis_pipeline_init(&js->pipelines[i],
                           &product->pipeline,
                           product->axis_map[i].calibrate);
    }
}

void joystick_update(joystick_t *js) {
    for (uint8_t i = 0; i < js->axes_count; i++) {
        uint16_t raw;
        js->axis_sources[i].read_raw(js->axis_sources[i].ctx, &raw);
        axis_pipeline_process(&js->pipelines[i], raw);
        js->data.axes[i] = js->pipelines[i].output;
    }
    js->btn_source.read_buttons(js->btn_source.ctx, &js->data.buttons);
}
```

---

## 六、协议层架构（Profile 驱动 + Protocol Bundle）

### 6.1 设计动机

原设计中协议插件存在三个问题：

1. **`void *config`** — `init(const void *config)` 无类型检查，传错 config 编译通过、运行崩溃
2. **协议行为 hardcode** — OD 索引、PGN、字节打包、值范围全写死在代码里，换品牌兼容就要改代码
3. **标准 vs 魔改 = 代码分支** — 兼容不同品牌导致 `if/else` 蔓延

改进思路：**协议行为由 const 数据（Profile）驱动，协议插件变成通用引擎；产品通过 Protocol Bundle（tagged union）选择协议+Profile，编译期类型安全。**

### 6.2 通用数据模型

```c
// app/joystick_data.h
#pragma once
#include <stdint.h>

#define MAX_AXES    4
#define MAX_BUTTONS 12

typedef struct {
    int32_t  axes[MAX_AXES];       // 信号管线输出（校准后有符号偏移）
    uint8_t  axes_count;
    uint8_t  axes_valid;           // 位掩码：哪些轴数据有效
    int32_t  max_deflection;       // 最大偏转量（用于协议层缩放）
    uint16_t buttons;              // 位掩码
    uint8_t  buttons_count;
    uint32_t timestamp_ms;
    uint8_t  safety_state;         // 0=正常 1=降级 2=安全停止
    uint8_t  error_flags;          // 传感器故障等
} joystick_data_t;

typedef struct {
    uint8_t  comm_ok;
    uint8_t  bus_off;
    uint32_t tx_count;
    uint32_t rx_count;
    uint32_t error_count;
} protocol_status_t;
```

### 6.3 轴输出映射（通用）

所有协议共享的轴值转换描述，定义目标范围和方向：

```c
// protocols/axis_output_map.h
#pragma once
#include <stdint.h>

typedef struct {
    int32_t output_min;     // 目标最小值（如 -10000 / 0）
    int32_t output_max;     // 目标最大值（如 +10000 / 65535）
    int8_t  direction;      // +1 正常, -1 反向
} axis_output_map_t;

// 通用转换：管线输出 → 协议目标范围
static inline int32_t axis_map_output(int32_t val, int32_t max_deflection,
                                       const axis_output_map_t *map) {
    int32_t center = (map->output_min + map->output_max) / 2;
    int32_t half   = (map->output_max - map->output_min) / 2;
    return center + val * map->direction * half / max_deflection;
}
```

### 6.4 CANopen Profile — OD + PDO + 值映射

将 CANopen 的所有品牌差异（OD 布局、PDO 映射、值范围、时序、EMCY 代码）抽成一张描述表：

```c
// protocols/canopen/canopen_profile.h
#pragma once
#include "axis_output_map.h"

// ---- 对象字典条目描述 ----
typedef enum { OD_UINT8, OD_UINT16, OD_UINT32, OD_INT16, OD_INT32 } od_data_type_t;
typedef enum { OD_RO, OD_WO, OD_RW, OD_CONST } od_access_t;

typedef struct {
    uint16_t       index;
    uint8_t        subindex;
    od_data_type_t type;
    od_access_t    access;
    uint16_t       data_offset;   // 在 od_runtime_t.data[] 中的偏移
    uint32_t       default_val;
} od_entry_t;

typedef struct {
    uint8_t data[128];            // 所有 OD 条目的运行时值
} od_runtime_t;

typedef struct {
    const od_entry_t *entries;
    uint16_t          entry_count;
    od_runtime_t     *runtime;
} od_table_t;

// ---- TPDO 映射描述 ----
typedef struct {
    uint16_t od_index;
    uint8_t  od_subindex;
    uint8_t  bit_length;
} tpdo_mapping_entry_t;

typedef struct {
    tpdo_mapping_entry_t entries[8];
    uint8_t count;
    uint16_t event_timer_ms;
} tpdo_config_t;

// ---- CANopen Profile（完整品牌描述） ----
typedef struct {
    const char              *profile_name;
    const od_table_t        *od;
    const tpdo_config_t      tpdos[4];
    uint8_t                  tpdo_count;
    const axis_output_map_t *axis_maps;
    uint8_t                  axis_map_count;
    uint16_t                 heartbeat_ms;
    uint16_t                 emcy_code_sensor_fault;
    bool                     has_sdo_params;   // 是否支持主站在线改参
} canopen_profile_t;
```

标准 DS401 profile 示例：

```c
// config/canopen_profiles/ds401_standard.c
static const od_entry_t od_ds401_entries[] = {
    { 0x1000, 0, OD_UINT32, OD_RO, 0,  0x00020192 },  // Device type
    { 0x1001, 0, OD_UINT8,  OD_RO, 4,  0          },  // Error register
    { 0x6401, 1, OD_INT16,  OD_RO, 5,  0          },  // Analog input 1
    { 0x6401, 2, OD_INT16,  OD_RO, 7,  0          },  // Analog input 2
    { 0x6000, 1, OD_UINT16, OD_RO, 9,  0          },  // Digital inputs
};
static od_runtime_t od_ram_ds401;
static const od_table_t od_ds401 = {
    .entries = od_ds401_entries, .entry_count = 5, .runtime = &od_ram_ds401,
};

static const axis_output_map_t ds401_axes[] = {
    { .output_min = -10000, .output_max = 10000, .direction = 1 },
    { .output_min = -10000, .output_max = 10000, .direction = 1 },
};

const canopen_profile_t canopen_ds401 = {
    .profile_name  = "DS401 Standard",
    .od            = &od_ds401,
    .tpdo_count    = 2,
    .tpdos = {
        [0] = { .entries = {{ 0x6401, 1, 16 }, { 0x6401, 2, 16 }},
                .count = 2, .event_timer_ms = 20 },
        [1] = { .entries = {{ 0x6000, 1, 16 }},
                .count = 1, .event_timer_ms = 50 },
    },
    .axis_maps     = ds401_axes,
    .axis_map_count = 2,
    .heartbeat_ms  = 1000,
    .emcy_code_sensor_fault = 0x5000,
    .has_sdo_params = false,
};
```

"魔改"品牌 profile（私有 OD 索引 + SDO 可配参数）：

```c
// config/canopen_profiles/brand_b_compat.c
static const od_entry_t od_brand_b_entries[] = {
    { 0x1000, 0, OD_UINT32, OD_RO, 0,  0x00020192 },
    { 0x1001, 0, OD_UINT8,  OD_RO, 4,  0          },
    // 私有轴数据区（无符号）
    { 0x2100, 1, OD_UINT16, OD_RO, 5,  0          },
    { 0x2100, 2, OD_UINT16, OD_RO, 7,  0          },
    { 0x2200, 1, OD_UINT16, OD_RO, 9,  0          },
    // 魔改：SDO 可配参数
    { 0x2300, 1, OD_UINT16, OD_RW, 11, 300        },  // 死区（主站可调）
    { 0x2300, 2, OD_UINT8,  OD_RW, 13, 200        },  // 滤波系数
    { 0x2300, 3, OD_UINT8,  OD_RW, 14, 0          },  // 校准命令
};
static od_runtime_t od_ram_brand_b;
static const od_table_t od_brand_b = {
    .entries = od_brand_b_entries, .entry_count = 8, .runtime = &od_ram_brand_b,
};

static const axis_output_map_t brand_b_axes[] = {
    { .output_min = 0, .output_max = 65535, .direction =  1 },
    { .output_min = 0, .output_max = 65535, .direction = -1 },  // Y 反向
};

const canopen_profile_t canopen_brand_b = {
    .profile_name  = "Brand-B Compatible",
    .od            = &od_brand_b,
    .tpdo_count    = 1,
    .tpdos = {
        [0] = { .entries = {{ 0x2100, 1, 16 }, { 0x2100, 2, 16 }, { 0x2200, 1, 16 }},
                .count = 3, .event_timer_ms = 10 },
    },
    .axis_maps     = brand_b_axes,
    .axis_map_count = 2,
    .heartbeat_ms  = 500,
    .emcy_code_sensor_fault = 0x8000,
    .has_sdo_params = true,
};
```

### 6.5 CANopen 通用引擎（Profile 驱动，不因品牌改变）

```c
// protocols/canopen/canopen_engine.c
#include "canopen_profile.h"
#include "canopen_stack.h"

static const canopen_profile_t *profile;
static const can_driver_t *can;
static uint8_t node_id;

drv_status_t canopen_engine_init(const canopen_config_t *cfg) {
    can = cfg->can;
    node_id = cfg->node_id;
    profile = cfg->profile;

    co_node_init(node_id);
    co_nmt_set_heartbeat(profile->heartbeat_ms);

    // 根据 profile 初始化 OD 默认值
    od_load_defaults(profile->od);

    // 根据 profile 配置 TPDO 映射
    for (uint8_t t = 0; t < profile->tpdo_count; t++) {
        const tpdo_config_t *tp = &profile->tpdos[t];
        for (uint8_t e = 0; e < tp->count; e++) {
            co_tpdo_map(t + 1, tp->entries[e].od_index,
                        tp->entries[e].od_subindex,
                        tp->entries[e].bit_length);
        }
        co_tpdo_set_event_timer(t + 1, tp->event_timer_ms);
    }

    event_bus_subscribe(EVT_SAFETY_STATE_CHANGED, on_safety_changed);
    return DRV_OK;
}

drv_status_t canopen_engine_send(const joystick_data_t *data) {
    // 按 profile 的 axis_maps 转换并写入 OD
    for (uint8_t i = 0; i < profile->axis_map_count && i < data->axes_count; i++) {
        int32_t mapped = axis_map_output(
            data->axes[i], data->max_deflection, &profile->axis_maps[i]);
        od_update_value(profile->od,
                        profile->tpdos[0].entries[i].od_index,
                        profile->tpdos[0].entries[i].od_subindex,
                        mapped);
    }
    return DRV_OK;
}

drv_status_t canopen_engine_process(void) {
    co_node_process();
    return DRV_OK;
}

static void on_safety_changed(const event_t *evt) {
    if (evt->data.safety_state == SAFETY_STOP) {
        co_emcy_send(profile->emcy_code_sensor_fault);
    }
}
```

### 6.6 J1939 Profile — PGN + SPN 描述符

J1939 没有 OD，变化点是 PGN 选择和帧内字节打包方式。用 SPN 描述符定义：

```c
// protocols/j1939/j1939_profile.h
#pragma once
#include <stdint.h>

typedef enum {
    SPN_SOURCE_AXIS,       // 来源：轴数据
    SPN_SOURCE_BUTTON,     // 来源：按键位掩码
    SPN_SOURCE_SAFETY,     // 来源：安全状态
    SPN_SOURCE_CONST,      // 来源：固定值（如 0xFF 填充）
} spn_source_t;

typedef struct {
    spn_source_t source;
    uint8_t  source_index;    // 轴/按键编号（CONST 时为固定值）
    uint8_t  byte_offset;     // 帧中起始字节（0-7）
    uint8_t  bit_offset;      // 字节内起始位（0-7）
    uint8_t  bit_length;      // 位宽
    int32_t  scale_num;       // 缩放分子
    int32_t  scale_den;       // 缩放分母（physical = raw * num / den + offset）
    int32_t  add_offset;      // 偏移量
    int8_t   direction;       // +1 正常, -1 反向
} spn_descriptor_t;

typedef struct {
    uint32_t pgn;
    uint8_t  priority;
    uint16_t tx_interval_ms;
    const spn_descriptor_t *spns;
    uint8_t  spn_count;
} pgn_descriptor_t;

typedef struct {
    const char             *profile_name;
    uint8_t                 source_address;
    const pgn_descriptor_t *pgns;
    uint8_t                 pgn_count;
} j1939_profile_t;
```

标准 profile 与魔改 profile 的差异仅在数据定义：

```c
// config/j1939_profiles/j1939_standard.c
static const spn_descriptor_t std_joy_spns[] = {
    { SPN_SOURCE_AXIS,   0, 0, 0, 16, 65535, 4096, 0,  1 },  // X: 字节 0-1
    { SPN_SOURCE_AXIS,   1, 2, 0, 16, 65535, 4096, 0,  1 },  // Y: 字节 2-3
    { SPN_SOURCE_BUTTON, 0, 4, 0, 16, 1,     1,    0,  1 },  // 按键: 字节 4-5
    { SPN_SOURCE_SAFETY, 0, 6, 0, 8,  1,     1,    0,  1 },  // 安全: 字节 6
    { SPN_SOURCE_CONST,  0xFF, 7, 0, 8, 1,   1,    0,  1 },  // 填充: 字节 7
};
static const pgn_descriptor_t std_pgns[] = {
    { .pgn = 0xEF00, .priority = 6, .tx_interval_ms = 20,
      .spns = std_joy_spns, .spn_count = 5 },
};
const j1939_profile_t j1939_standard = {
    .profile_name = "J1939 Standard", .source_address = 0x27,
    .pgns = std_pgns, .pgn_count = 1,
};

// config/j1939_profiles/j1939_brand_x.c — 魔改：Y 在前、方向反、10-bit
static const spn_descriptor_t bx_joy_spns[] = {
    { SPN_SOURCE_AXIS,   1, 0, 0, 10, 1023, 4096, 0, -1 },  // Y 在前，反向
    { SPN_SOURCE_AXIS,   0, 1, 2, 10, 1023, 4096, 0,  1 },  // X 在后，跨字节
    { SPN_SOURCE_BUTTON, 0, 3, 0, 8,  1,    1,    0,  1 },  // 按键: 仅 8-bit
};
static const pgn_descriptor_t bx_pgns[] = {
    { .pgn = 0xFF21, .priority = 3, .tx_interval_ms = 10,
      .spns = bx_joy_spns, .spn_count = 3 },
};
const j1939_profile_t j1939_brand_x = {
    .profile_name = "Brand-X Compatible", .source_address = 0x30,
    .pgns = bx_pgns, .pgn_count = 1,
};
```

### 6.7 J1939 通用引擎（SPN 描述符驱动打包）

```c
// protocols/j1939/j1939_engine.c
#include "j1939_profile.h"

static const j1939_profile_t *profile;
static const can_driver_t *can;

drv_status_t j1939_engine_init(const j1939_config_t *cfg) {
    can = cfg->can;
    profile = cfg->profile;
    return DRV_OK;
}

drv_status_t j1939_engine_send(const joystick_data_t *data) {
    for (uint8_t p = 0; p < profile->pgn_count; p++) {
        const pgn_descriptor_t *pgn = &profile->pgns[p];
        can_frame_t frame = {0};
        frame.id = ((uint32_t)pgn->priority << 26)
                 | (pgn->pgn << 8)
                 | profile->source_address;
        frame.is_extended = 1;
        frame.dlc = 8;
        memset(frame.data, 0xFF, 8);

        for (uint8_t s = 0; s < pgn->spn_count; s++) {
            const spn_descriptor_t *spn = &pgn->spns[s];
            int32_t raw = spn_get_source(data, spn);
            uint32_t packed = (uint32_t)(raw * spn->direction
                              * spn->scale_num / spn->scale_den
                              + spn->add_offset);
            spn_write_bits(frame.data, packed,
                           spn->byte_offset, spn->bit_offset, spn->bit_length);
        }
        can->send(&frame);
    }
    return DRV_OK;
}

static int32_t spn_get_source(const joystick_data_t *data,
                              const spn_descriptor_t *spn) {
    switch (spn->source) {
        case SPN_SOURCE_AXIS:    return data->axes[spn->source_index];
        case SPN_SOURCE_BUTTON:  return data->buttons;
        case SPN_SOURCE_SAFETY:  return data->safety_state;
        case SPN_SOURCE_CONST:   return spn->source_index;
        default:                 return 0;
    }
}
```

### 6.8 模拟量输出引擎

模拟量输出无需 profile（变化仅在电压范围，直接放 config）：

```c
// protocols/analog/analog_engine.c
static analog_config_t cfg;

drv_status_t analog_engine_init(const analog_config_t *c) {
    cfg = *c;
    return DRV_OK;
}

drv_status_t analog_engine_send(const joystick_data_t *data) {
    for (uint8_t i = 0; i < data->axes_count; i++) {
        int32_t mv = cfg.center_mv
                   + data->axes[i] * (int32_t)(cfg.max_mv - cfg.center_mv)
                     / data->max_deflection;
        if (mv < cfg.min_mv) mv = cfg.min_mv;
        if (mv > cfg.max_mv) mv = cfg.max_mv;
        cfg.dac->write_mv(i, (uint32_t)mv);
    }
    return DRV_OK;
}
```

### 6.9 Protocol Bundle（类型安全的协议配置包）

用 tagged union 替代 `void *config`，编译期保证协议与配置匹配：

```c
// protocols/protocol_bundle.h
#pragma once
#include "canopen_profile.h"
#include "j1939_profile.h"

typedef enum {
    PROTOCOL_TYPE_CANOPEN,
    PROTOCOL_TYPE_J1939,
    PROTOCOL_TYPE_ANALOG,
} protocol_type_t;

typedef struct {
    const can_driver_t      *can;
    uint8_t                  node_id;
    const canopen_profile_t *profile;
} canopen_config_t;

typedef struct {
    const can_driver_t      *can;
    const j1939_profile_t   *profile;
} j1939_config_t;

typedef struct {
    const dac_driver_t      *dac;
    uint16_t                 min_mv;
    uint16_t                 max_mv;
    uint16_t                 center_mv;
} analog_config_t;

typedef struct {
    protocol_type_t type;
    union {
        canopen_config_t canopen;
        j1939_config_t   j1939;
        analog_config_t  analog;
    };
} protocol_bundle_t;
```

### 6.10 协议能力与运行时接口

能力位掩码保留——运行时查询"支不支持诊断"仍然有用：

```c
// protocols/protocol_runtime.h
typedef enum {
    PROTO_CAP_RECEIVABLE    = (1 << 0),
    PROTO_CAP_CONFIGURABLE  = (1 << 1),
    PROTO_CAP_DIAGNOSABLE   = (1 << 2),
    PROTO_CAP_SAFETY        = (1 << 3),
} protocol_capability_t;

typedef struct {
    protocol_type_t type;
    uint32_t capabilities;

    drv_status_t (*send)(const joystick_data_t *data);
    drv_status_t (*process)(void);
    drv_status_t (*get_bus_diag)(protocol_status_t *status);
} protocol_runtime_t;
```

初始化时根据 bundle type 构建运行时接口（唯一的 switch，启动时执行一次）：

```c
// protocols/protocol_init.c
static protocol_runtime_t runtime;

drv_status_t protocol_init(const protocol_bundle_t *bundle) {
    runtime.type = bundle->type;

    switch (bundle->type) {
        case PROTOCOL_TYPE_CANOPEN:
            canopen_engine_init(&bundle->canopen);
            runtime.send         = canopen_engine_send;
            runtime.process      = canopen_engine_process;
            runtime.get_bus_diag = canopen_engine_get_diag;
            runtime.capabilities = PROTO_CAP_RECEIVABLE
                                 | PROTO_CAP_CONFIGURABLE
                                 | PROTO_CAP_DIAGNOSABLE;
            break;

        case PROTOCOL_TYPE_J1939:
            j1939_engine_init(&bundle->j1939);
            runtime.send         = j1939_engine_send;
            runtime.process      = j1939_engine_process;
            runtime.get_bus_diag = NULL;
            runtime.capabilities = PROTO_CAP_RECEIVABLE;
            break;

        case PROTOCOL_TYPE_ANALOG:
            analog_engine_init(&bundle->analog);
            runtime.send         = analog_engine_send;
            runtime.process      = NULL;
            runtime.get_bus_diag = NULL;
            runtime.capabilities = 0;
            break;

        default:
            return DRV_ERR_INVALID;
    }
    return DRV_OK;
}

const protocol_runtime_t* protocol_get_runtime(void) {
    return &runtime;
}
```

运行时使用：

```c
void app_run_cycle(const joystick_data_t *data) {
    const protocol_runtime_t *proto = protocol_get_runtime();

    proto->send(data);

    if (proto->capabilities & PROTO_CAP_RECEIVABLE) {
        proto->process();
    }
    if (proto->capabilities & PROTO_CAP_DIAGNOSABLE) {
        protocol_status_t status;
        proto->get_bus_diag(&status);
    }
}
```

---

## 七、产品配置系统（OCP 改进：每产品独立文件）

### 7.1 产品描述符接口（永不修改）

```c
// config/product_descriptor.h — 定义接口，永不修改
#pragma once
#include <stdint.h>
#include "signal_pipeline.h"

// ---- 通道重映射：物理通道 → 逻辑功能 ----

typedef int32_t (*calibrate_fn_t)(uint16_t raw, const void *ctx);

typedef struct {
    uint8_t        physical_channel;  // scan_buffer 中的物理通道号
    uint8_t        logical_index;     // 逻辑轴编号
    calibrate_fn_t calibrate;         // 校准策略（calibrate_linear / calibrate_circular）
} axis_channel_map_t;

typedef struct {
    uint8_t physical_channel;         // scan_buffer 中的物理通道号
    uint8_t logical_index;            // 逻辑按键编号
} btn_channel_map_t;

// ---- 信号管线参数（每产品配置） ----

typedef struct {
    uint16_t       fault_threshold_low;
    uint16_t       fault_threshold_high;
    uint8_t        filter_alpha;       // 0 = 不启用滤波
    int32_t        deadzone_threshold;
    deadzone_fn_t  deadzone_fn;        // 死区策略（linear / scurve / exponential）
} axis_pipeline_config_t;

// ---- 产品描述符 ----

typedef struct {
    const char *model_name;
    uint8_t    resolution_bits;         // 传感器分辨率（12 / 14）
    uint8_t    has_knob;
    uint8_t    has_force_feedback;
    uint8_t    has_dac_output;
    uint8_t    has_safety;

    // 通道重映射
    const axis_channel_map_t *axis_map;
    uint8_t                   axis_map_count;
    const btn_channel_map_t  *btn_map;
    uint8_t                   btn_map_count;

    // 信号管线参数（所有轴共用，或未来可扩展为 per-axis）
    axis_pipeline_config_t    pipeline;
} product_descriptor_t;

// 唯一的外部声明 — 由具体产品配置文件定义
extern const product_descriptor_t product_config;
```

### 7.2 各产品独立配置文件

新增产品只需新增文件，不修改任何已有代码：

```c
// config/products/js100.c — 单轴模拟量摇杆，DAC 输出
#include "product_descriptor.h"
#include "calibrator.h"

static const axis_channel_map_t js100_axes[] = {
    { .physical_channel = 0, .logical_index = 0, .calibrate = calibrate_linear },
};

static const btn_channel_map_t js100_btns[] = {
    { .physical_channel = 2, .logical_index = 0 },
    { .physical_channel = 3, .logical_index = 1 },
};

const product_descriptor_t product_config = {
    .model_name        = "JS-100",
    .resolution_bits   = 12,
    .has_dac_output    = 1,
    .axis_map          = js100_axes,
    .axis_map_count    = 1,
    .btn_map           = js100_btns,
    .btn_map_count     = 2,
    .pipeline = {
        .fault_threshold_low  = 50,
        .fault_threshold_high = 4046,
        .filter_alpha         = 0,           // 无滤波
        .deadzone_threshold   = 300,
        .deadzone_fn          = deadzone_linear,
    },
};
```

```c
// config/products/js200.c — 双轴模拟量摇杆，CAN 输出
#include "product_descriptor.h"
#include "calibrator.h"

static const axis_channel_map_t js200_axes[] = {
    { .physical_channel = 0, .logical_index = 0, .calibrate = calibrate_linear },
    { .physical_channel = 1, .logical_index = 1, .calibrate = calibrate_linear },
};

static const btn_channel_map_t js200_btns[] = {
    { .physical_channel = 2, .logical_index = 0 },
    { .physical_channel = 3, .logical_index = 1 },
    { .physical_channel = 4, .logical_index = 2 },
    { .physical_channel = 5, .logical_index = 3 },
};

const product_descriptor_t product_config = {
    .model_name        = "JS-200",
    .resolution_bits   = 12,
    .axis_map          = js200_axes,
    .axis_map_count    = 2,
    .btn_map           = js200_btns,
    .btn_map_count     = 4,
    .pipeline = {
        .fault_threshold_low  = 50,
        .fault_threshold_high = 4046,
        .filter_alpha         = 200,
        .deadzone_threshold   = 400,
        .deadzone_fn          = deadzone_linear,
    },
};
```

```c
// config/products/js300.c — 三轴霍尔摇杆 + 旋钮，安全型
#include "product_descriptor.h"
#include "calibrator.h"

static const axis_channel_map_t js300_axes[] = {
    { .physical_channel = 0, .logical_index = 0, .calibrate = calibrate_circular },  // X 霍尔
    { .physical_channel = 1, .logical_index = 1, .calibrate = calibrate_circular },  // Y 霍尔
    { .physical_channel = 2, .logical_index = 2, .calibrate = calibrate_linear },    // 旋钮（电位器）
};

static const btn_channel_map_t js300_btns[] = {
    { .physical_channel = 3, .logical_index = 0 },
    { .physical_channel = 4, .logical_index = 1 },
    { .physical_channel = 5, .logical_index = 2 },
    { .physical_channel = 6, .logical_index = 3 },
    { .physical_channel = 7, .logical_index = 4 },
    { .physical_channel = 8, .logical_index = 5 },
    { .physical_channel = 9, .logical_index = 6 },
    { .physical_channel = 10, .logical_index = 7 },
};

const product_descriptor_t product_config = {
    .model_name        = "JS-300",
    .resolution_bits   = 14,
    .has_safety        = 1,
    .axis_map          = js300_axes,
    .axis_map_count    = 3,
    .btn_map           = js300_btns,
    .btn_map_count     = 8,
    .pipeline = {
        .fault_threshold_low  = 50,
        .fault_threshold_high = 16330,
        .filter_alpha         = 220,
        .deadzone_threshold   = 500,
        .deadzone_fn          = deadzone_scurve,
    },
};
```

```c
// config/products/js400.c — 双轴霍尔 + 力反馈，同硬件不同通道映射示例
#include "product_descriptor.h"
#include "calibrator.h"

// 与 JS-300 共用同一 PCB，但通道映射不同：
// 物理通道 0→逻辑轴 Y（反），物理通道 1→逻辑轴 X
static const axis_channel_map_t js400_axes[] = {
    { .physical_channel = 1, .logical_index = 0, .calibrate = calibrate_circular },  // CH1 → X
    { .physical_channel = 0, .logical_index = 1, .calibrate = calibrate_circular },  // CH0 → Y
};

static const btn_channel_map_t js400_btns[] = {
    { .physical_channel = 3,  .logical_index = 0 },
    { .physical_channel = 4,  .logical_index = 1 },
    { .physical_channel = 5,  .logical_index = 2 },
    { .physical_channel = 6,  .logical_index = 3 },
    { .physical_channel = 7,  .logical_index = 4 },
    { .physical_channel = 8,  .logical_index = 5 },
    { .physical_channel = 9,  .logical_index = 6 },
    { .physical_channel = 10, .logical_index = 7 },
    { .physical_channel = 11, .logical_index = 8 },
    { .physical_channel = 12, .logical_index = 9 },
    { .physical_channel = 13, .logical_index = 10 },
    { .physical_channel = 14, .logical_index = 11 },
};

const product_descriptor_t product_config = {
    .model_name        = "JS-400",
    .resolution_bits   = 14,
    .has_force_feedback = 1,
    .has_safety        = 1,
    .axis_map          = js400_axes,
    .axis_map_count    = 2,
    .btn_map           = js400_btns,
    .btn_map_count     = 12,
    .pipeline = {
        .fault_threshold_low  = 50,
        .fault_threshold_high = 16330,
        .filter_alpha         = 240,
        .deadzone_threshold   = 400,
        .deadzone_fn          = deadzone_exponential,
    },
};
```

---

## 八、事件总线（观察者模式）

### 8.1 设计动机

原设计中协议插件和安全监控之间没有通信机制。实际场景需求：
- CAN Bus-Off 发生时 → 安全监控需要被通知
- 安全状态变化时 → 协议层需要发送紧急消息
- 传感器故障时 → 诊断模块需要记录日志

事件总线提供了松耦合的发布/订阅机制。

### 8.2 事件总线接口

```c
// event/event_bus.h
#pragma once
#include <stdint.h>

typedef enum {
    EVT_SENSOR_FAULT,
    EVT_CAN_BUS_OFF,
    EVT_CAN_RECOVERED,
    EVT_SAFETY_STATE_CHANGED,
    EVT_CALIBRATION_COMPLETE,
    EVT_WATCHDOG_WARNING,
    EVT_DEVICE_STATE_CHANGED,
    EVT_MAX
} event_type_t;

typedef struct {
    event_type_t type;
    uint32_t     timestamp;
    union {
        uint8_t  sensor_id;
        uint8_t  safety_state;  // 0=normal, 1=degraded, 2=safe-stop
        uint32_t error_code;
        uint8_t  new_device_state;
    } data;
} event_t;

typedef void (*event_handler_t)(const event_t *evt);

#define EVENT_BUS_MAX_SUBSCRIBERS 4  // 每个事件类型最多 4 个订阅者

void event_bus_init(void);
void event_bus_subscribe(event_type_t type, event_handler_t handler);
void event_bus_publish(const event_t *evt);
```

```c
// event/event_bus.c
#include "event_bus.h"

static struct {
    event_handler_t handlers[EVENT_BUS_MAX_SUBSCRIBERS];
    uint8_t count;
} subscribers[EVT_MAX];

void event_bus_init(void) {
    for (uint8_t i = 0; i < EVT_MAX; i++) {
        subscribers[i].count = 0;
    }
}

void event_bus_subscribe(event_type_t type, event_handler_t handler) {
    if (type < EVT_MAX && subscribers[type].count < EVENT_BUS_MAX_SUBSCRIBERS) {
        subscribers[type].handlers[subscribers[type].count++] = handler;
    }
}

void event_bus_publish(const event_t *evt) {
    if (evt->type < EVT_MAX) {
        for (uint8_t i = 0; i < subscribers[evt->type].count; i++) {
            subscribers[evt->type].handlers[i](evt);
        }
    }
}
```

### 8.3 各模块的事件订阅

```c
// 安全监控订阅 CAN 事件和传感器事件
void safety_monitor_init(...) {
    event_bus_subscribe(EVT_CAN_BUS_OFF, on_can_bus_off);
    event_bus_subscribe(EVT_SENSOR_FAULT, on_sensor_fault);
}

static void on_can_bus_off(const event_t *evt) {
    // CAN 总线故障 → 进入降级模式
    safety_enter_degraded_mode();
}

// CANopen 协议订阅安全事件
static void on_safety_changed(const event_t *evt) {
    if (evt->data.safety_state == 2) {  // SAFE_STOP
        co_emcy_send(0x5000);           // 发送紧急消息
    }
}

// 诊断模块订阅所有故障事件
void diagnostics_init(void) {
    event_bus_subscribe(EVT_SENSOR_FAULT, diag_log_fault);
    event_bus_subscribe(EVT_CAN_BUS_OFF, diag_log_fault);
    event_bus_subscribe(EVT_WATCHDOG_WARNING, diag_log_fault);
}
```

---

## 九、设备状态机

### 9.1 设计动机

原设计缺少明确的设备生命周期状态管理。实际嵌入式产品需要管理启动自检、预操作、正常运行、安全停止、校准、固件更新等状态。

### 9.2 状态机定义

```c
// app/device_state.h
#pragma once
#include "event_bus.h"

typedef enum {
    STATE_BOOT,            // 启动自检
    STATE_PRE_OPERATIONAL, // 初始化完成，等待使能
    STATE_OPERATIONAL,     // 正常工作
    STATE_SAFE_STOP,       // 安全停止（故障触发）
    STATE_CALIBRATION,     // 校准模式
    STATE_FIRMWARE_UPDATE, // 固件更新
    STATE_MAX
} device_state_t;

typedef struct {
    device_state_t current;
    drv_status_t (*on_enter)(void);
    drv_status_t (*on_execute)(void);
    drv_status_t (*on_exit)(void);

    // 状态转移表
    struct {
        event_type_t   trigger;
        device_state_t next;
        bool (*guard)(void);  // 转移守卫条件
    } transitions[8];
    uint8_t transition_count;
} state_handler_t;

void device_state_init(void);
void device_state_process(void);
device_state_t device_state_get_current(void);
```

```c
// app/device_state.c

static state_handler_t state_table[STATE_MAX];
static device_state_t  current_state = STATE_BOOT;

void device_state_init(void) {
    // 注册状态处理器
    state_table[STATE_BOOT] = (state_handler_t){
        .on_enter   = boot_self_test,
        .on_execute = boot_check_complete,
        .transitions = {
            { .trigger = EVT_DEVICE_STATE_CHANGED, .next = STATE_PRE_OPERATIONAL,
              .guard = boot_test_passed },
            { .trigger = EVT_SENSOR_FAULT, .next = STATE_SAFE_STOP },
        },
        .transition_count = 2,
    };

    state_table[STATE_OPERATIONAL] = (state_handler_t){
        .on_enter   = operational_enter,
        .on_execute = operational_run,
        .on_exit    = operational_exit,
        .transitions = {
            { .trigger = EVT_SAFETY_STATE_CHANGED, .next = STATE_SAFE_STOP },
            { .trigger = EVT_CALIBRATION_COMPLETE, .next = STATE_CALIBRATION,
              .guard = calibration_requested },
        },
        .transition_count = 2,
    };

    // ... 其他状态配置

    // 订阅所有可能触发转移的事件
    event_bus_subscribe(EVT_SAFETY_STATE_CHANGED, on_state_event);
    event_bus_subscribe(EVT_SENSOR_FAULT, on_state_event);
    event_bus_subscribe(EVT_DEVICE_STATE_CHANGED, on_state_event);
}

static void on_state_event(const event_t *evt) {
    state_handler_t *handler = &state_table[current_state];
    for (uint8_t i = 0; i < handler->transition_count; i++) {
        if (handler->transitions[i].trigger == evt->type) {
            if (!handler->transitions[i].guard
                || handler->transitions[i].guard()) {
                // 执行状态转移
                if (handler->on_exit) handler->on_exit();
                current_state = handler->transitions[i].next;
                state_handler_t *next = &state_table[current_state];
                if (next->on_enter) next->on_enter();
                break;
            }
        }
    }
}

void device_state_process(void) {
    if (state_table[current_state].on_execute) {
        state_table[current_state].on_execute();
    }
}
```

---

## 十、应用主循环（SRP 改进：组装与运行分离）

原设计 `main_app.c` 同时负责依赖组装（Composition Root）和业务循环。改进后将两者分离。

### 10.1 组装入口（Composition Root）

```c
// app/app_compose.c — 只负责依赖组装，不包含业务逻辑

#include "product_descriptor.h"
#include "platform_registry.h"
#include "protocol_bundle.h"
#include "protocol_init.h"
#include "event_bus.h"
#include "device_state.h"

static joystick_t      joystick;
static scan_buffer_t   scan_buf;
static board_scanner_t scanner;

void app_compose(void) {
    // 1. 初始化事件总线
    event_bus_init();

    // 2. 初始化设备状态机
    device_state_init();

    // 3. 初始化板级扫描器
    board_scan_init(&scanner, &product_config);

    // 4. 根据通道映射表组装输入源（物理通道 → 逻辑功能）
    for (uint8_t i = 0; i < product_config.axis_map_count; i++) {
        const axis_channel_map_t *m = &product_config.axis_map[i];
        joystick.axis_sources[m->logical_index] =
            create_buf_axis_source(&scan_buf, m->physical_channel);
        joystick.axis_calibrate[m->logical_index] = m->calibrate;
    }
    for (uint8_t i = 0; i < product_config.btn_map_count; i++) {
        const btn_channel_map_t *m = &product_config.btn_map[i];
        joystick.btn_physical_map[m->logical_index] = m->physical_channel;
    }
    joystick.axes_count = product_config.axis_map_count;
    joystick.btns_count = product_config.btn_map_count;

    // 5. 初始化协议（Protocol Bundle，类型安全，无 void*）
    protocol_init(product_config.protocol);

    // 6. 如果是安全型号，初始化安全监控
    if (product_config.has_safety) {
        safety_monitor_init(platform_get_adc_voltage(),
                            platform_get_gpio_reader());
    }

    // 7. 初始化诊断
    diagnostics_init();
}

joystick_t*      app_get_joystick(void)  { return &joystick; }
board_scanner_t* app_get_scanner(void)   { return &scanner; }
scan_buffer_t*   app_get_scan_buf(void)  { return &scan_buf; }
```

### 10.2 运行循环 → RTOS 任务（见第十一节）

原设计中 `app_run()` 是一个裸 `for(;;)` 循环，所有逻辑串行执行在同一优先级。这在实际产品中存在严重问题：

- 安全监控被低优先级逻辑阻塞（违反功能安全时序要求）
- 协议栈处理（CANopen NMT/SDO/心跳）得不到及时响应
- 无法利用 ISR 到任务的上下文分离

改进方案见第十一节 RTOS 任务架构。

---

## 十一、RTOS 任务架构

### 11.1 设计动机

工业摇杆固件必须使用 RTOS，原因：

| 需求 | 裸循环问题 | RTOS 解决方案 |
|------|-----------|--------------|
| 安全监控 2ms 响应 | 被协议处理 or 信号链阻塞 | 高优先级任务抢占 |
| CANopen 心跳 1ms 精度 | 主循环抖动大 | 独立任务 + 硬件定时器 |
| 看门狗喂狗时序 | 长处理链导致超时 | 独立高优先级任务 |
| CAN 接收实时性 | 轮询方式丢帧 | ISR + 队列 + 任务 |

### 11.2 RTOS 抽象层

为支持 FreeRTOS / ThreadX 等不同 RTOS，定义统一抽象接口：

```c
// os/os_abstraction.h — RTOS 抽象层接口
#pragma once
#include <stdint.h>
#include <stdbool.h>

// ---- 任务管理 ----
typedef void *os_task_handle_t;
typedef void (*os_task_func_t)(void *param);

os_task_handle_t os_task_create(const char *name,
                                 os_task_func_t entry,
                                 void *param,
                                 uint16_t stack_words,
                                 uint8_t priority);

// ---- 互斥量 ----
typedef void *os_mutex_handle_t;
os_mutex_handle_t os_mutex_create(void);
bool os_mutex_lock(os_mutex_handle_t m, uint32_t timeout_ms);
void os_mutex_unlock(os_mutex_handle_t m);

// ---- 消息队列 ----
typedef void *os_queue_handle_t;
os_queue_handle_t os_queue_create(uint8_t item_count, uint16_t item_size);
bool os_queue_send(os_queue_handle_t q, const void *item, uint32_t timeout_ms);
bool os_queue_send_from_isr(os_queue_handle_t q, const void *item);
bool os_queue_receive(os_queue_handle_t q, void *item, uint32_t timeout_ms);

// ---- 定时与延时 ----
void     os_delay(uint32_t ms);
uint32_t os_tick_get(void);

// ---- 内核控制 ----
void os_kernel_init(void);
void os_kernel_start(void);  // 不返回
```

各 RTOS 对应一个实现文件（如 `os/os_freertos.c`、`os/os_threadx.c`），由 CMake 选择链接。

### 11.3 任务分解

```
┌─────────────────────────────────────────────────────────────────┐
│                        RTOS 任务架构                             │
├────────────┬──────────┬────────┬────────────────────────────────┤
│ 任务名      │ 优先级    │ 周期   │ 职责                           │
├────────────┼──────────┼────────┼────────────────────────────────┤
│ safety_task│ Highest  │ 2 ms   │ 安全监控、看门狗喂狗、故障响应   │
│ main_task  │ Normal   │ 10 ms  │ 摇杆采集→信号链→协议发送         │
│ proto_task │ Normal+1 │ 1 ms   │ 协议栈处理（NMT/SDO/心跳/RPDO） │
│ diag_task  │ Low      │ 100 ms │ 诊断日志、NV 存储刷写           │
│ idle_task  │ Idle     │ —      │ CPU 负载统计、低功耗管理         │
├────────────┼──────────┼────────┼────────────────────────────────┤
│ ISR: CAN RX│ 硬件中断  │ 事件   │ CAN 接收 → 入队列              │
│ ISR: ADC   │ 硬件中断  │ DMA    │ ADC DMA 完成 → 通知 main_task  │
│ ISR: Timer │ 硬件中断  │ 1 ms   │ RTOS 系统节拍                  │
└────────────┴──────────┴────────┴────────────────────────────────┘
```

### 11.4 任务创建与启动

```c
// app/app_tasks.c — RTOS 任务创建

#include "os_abstraction.h"
#include "app_compose.h"

void app_tasks_create(void) {
    // 组装依赖
    app_compose();

    // 创建任务（优先级数字越大越高）
    os_task_create("safety",  safety_task_entry,  NULL, 256, 5);
    os_task_create("main",    main_task_entry,    NULL, 512, 3);
    os_task_create("proto",   proto_task_entry,   NULL, 384, 4);
    os_task_create("diag",    diag_task_entry,    NULL, 256, 1);
}
```

### 11.5 各任务职责

```c
// safety_task — 最高优先级，不可被业务逻辑阻塞
static void safety_task_entry(void *param) {
    for (;;) {
        safety_monitor_update();     // 传感器交叉校验
        watchdog_feed();             // 喂狗
        device_state_process();      // 驱动状态机（安全事件触发转移）
        os_delay(2);                 // 2ms 周期
    }
}

// main_task — 摇杆数据采集与发送
static void main_task_entry(void *param) {
    joystick_t *js          = app_get_joystick();
    board_scanner_t *scanner = app_get_scanner();
    scan_buffer_t   *buf     = app_get_scan_buf();
    const protocol_runtime_t *proto = protocol_get_runtime();
    joystick_data_t data;

    for (;;) {
        if (device_state_get_current() == STATE_OPERATIONAL) {
            // 1. 板级扫描器一次性采集所有通道 → scan_buffer
            scanner->scan(scanner->ctx, buf);

            // 2. 摇杆更新：buf_axis_source / buf_btn_source 从 buffer 读取
            joystick_update(js);
            joystick_get_data(js, &data);

            os_mutex_lock(g_data_mutex, OS_WAIT_FOREVER);
            proto->send(&data);
            os_mutex_unlock(g_data_mutex);
        }
        os_delay(10);  // 10ms 周期
    }
}

// proto_task — 协议栈实时处理
static void proto_task_entry(void *param) {
    const protocol_runtime_t *proto = protocol_get_runtime();

    for (;;) {
        if (proto->capabilities & PROTO_CAP_RECEIVABLE) {
            proto->process();  // CANopen: NMT、SDO、心跳、RPDO
        }
        os_delay(1);  // 1ms 周期
    }
}

// diag_task — 低优先级后台服务
static void diag_task_entry(void *param) {
    for (;;) {
        diagnostics_update();
        nv_storage_flush();   // 将缓存写入 Flash/EEPROM
        logging_flush();
        os_delay(100);  // 100ms 周期
    }
}
```

### 11.6 共享数据保护

```
┌─────────────────────────────────────────────────────────┐
│                 共享资源保护策略                          │
├──────────────────┬──────────────────┬───────────────────┤
│ 共享数据          │ 写方              │ 保护方式           │
├──────────────────┼──────────────────┼───────────────────┤
│ joystick_data_t  │ main_task        │ mutex             │
│                  │ (读: proto_task)  │                   │
├──────────────────┼──────────────────┼───────────────────┤
│ protocol_status  │ proto_task       │ mutex             │
│                  │ (读: safety_task) │                   │
├──────────────────┼──────────────────┼───────────────────┤
│ device_state     │ safety_task      │ atomic / 单写者    │
│                  │ (读: all)        │                   │
├──────────────────┼──────────────────┼───────────────────┤
│ CAN RX 帧        │ ISR             │ os_queue          │
│                  │ (读: proto_task)  │ (ISR-safe 队列)   │
├──────────────────┼──────────────────┼───────────────────┤
│ event_bus 事件   │ 任意任务/ISR      │ 见下方说明         │
└──────────────────┴──────────────────┴───────────────────┘
```

### 11.7 事件总线的 ISR 安全

原有 `event_bus_publish()` 在回调中直接调用 handler，不适合从 ISR 发布。改进为**延迟分发**模式：

```c
// event/event_bus.h — ISR 安全版本

// 任务上下文：立即同步分发（原有行为）
void event_bus_publish(const event_t *evt);

// ISR 上下文：入队列，由指定任务延迟分发
void event_bus_publish_from_isr(const event_t *evt);

// 在 safety_task 中周期调用，处理 ISR 投递的事件
void event_bus_dispatch_deferred(void);
```

### 11.8 时序要求与优先级反转防范

```
时间线（最坏情况）：

0ms ─── safety_task 开始执行 (2ms 周期)
         安全监控 + 看门狗喂狗
0.3ms ── safety_task 完成，让出 CPU
         proto_task 获得 CPU (1ms 周期)
         CANopen 协议栈处理
0.8ms ── proto_task 完成
         main_task 获得 CPU (10ms 周期)
         摇杆采集 + 信号链 + 协议发送
2ms ──── safety_task 再次抢占 main_task
         确保安全监控不被阻塞
```

关键设计规则：
- `safety_task` 中**禁止使用 mutex**（避免优先级反转导致安全超时）
- 安全任务只读取数据，使用 atomic 或 volatile 访问
- CAN RX 中断处理时间 < 5μs（只做入队列）
- 所有 mutex 使用优先级继承协议

---

## 十二、构建系统（OCP + DIP 改进）

### 12.1 CMake 配置

```cmake
# CMakeLists.txt (顶层)

cmake_minimum_required(VERSION 3.20)

# ---- 产品和协议选择 ----
set(PRODUCT  "JS200"    CACHE STRING "Product model")
set(PROTOCOL "CANOPEN"  CACHE STRING "Communication protocol")
set(PLATFORM "STM32G4"  CACHE STRING "Target platform")

string(TOLOWER ${PRODUCT} PRODUCT_LOWER)

project(joystick_${PRODUCT}_${PROTOCOL} C ASM)

# ---- 编译定义（不再有 PRODUCT_JS200 这种宏） ----
add_compile_definitions(
    PROTOCOL_${PROTOCOL}
    PLATFORM_${PLATFORM}
)

# ---- OCP：每个产品一个独立的配置文件 ----
set(PRODUCT_CONFIG_SRC "config/products/${PRODUCT_LOWER}.c")
if(NOT EXISTS "${CMAKE_SOURCE_DIR}/${PRODUCT_CONFIG_SRC}")
    message(FATAL_ERROR "Unknown product: ${PRODUCT}")
endif()

# ---- 公共源文件 ----
set(APP_SOURCES
    app/app_compose.c
    app/app_run.c
    app/device_state.c
    event/event_bus.c
    input/buf_axis_source.c
    input/buf_btn_source.c
    board/board_scan.c
    signal/signal_chain.c
    signal/fault_detector.c
    signal/calibrator.c
    signal/filter.c
    signal/deadzone.c
    signal/debounce.c
    service/calibration.c
    service/diagnostics.c
    service/nv_storage.c
    platform/platform_registry.c
    ${PRODUCT_CONFIG_SRC}
)

# ---- 根据协议选择链接的协议插件 ----
if(PROTOCOL STREQUAL "CANOPEN")
    list(APPEND APP_SOURCES protocols/canopen/canopen_plugin.c)
    list(APPEND APP_SOURCES protocols/canopen/canopen_od.c)
elseif(PROTOCOL STREQUAL "J1939")
    list(APPEND APP_SOURCES protocols/j1939/j1939_plugin.c)
elseif(PROTOCOL STREQUAL "ANALOG")
    list(APPEND APP_SOURCES protocols/analog/analog_plugin.c)
endif()

# ---- 安全模块：根据产品配置自动判断 ----
# 不再用 #if defined(PRODUCT_JS300)，而是 CMake 读取产品属性
# 这里简化处理：JS300/JS400 包含安全模块
if(PRODUCT STREQUAL "JS300" OR PRODUCT STREQUAL "JS400")
    list(APPEND APP_SOURCES safety/safety_monitor.c)
    list(APPEND APP_SOURCES safety/stl_wrapper.c)
    list(APPEND APP_SOURCES safety/watchdog.c)
endif()

# ---- 根据平台选择驱动（DIP：链接器决定平台） ----
if(PLATFORM STREQUAL "STM32G4")
    include(cmake/stm32g4.cmake)
elseif(PLATFORM STREQUAL "S32K144")
    include(cmake/s32k144.cmake)
endif()

add_executable(${PROJECT_NAME} ${APP_SOURCES} ${PLATFORM_SOURCES})
```

### 12.2 批量构建

```bash
#!/bin/bash
# build_all.sh

CONFIGS=(
    "JS100 ANALOG  STM32G4"
    "JS200 CANOPEN STM32G4"
    "JS200 J1939   STM32G4"
    "JS300 CANOPEN STM32G4"
    "JS300 J1939   STM32G4"
    "JS400 CANOPEN STM32G4"
    "JS200 CANOPEN S32K144"
)

for cfg in "${CONFIGS[@]}"; do
    read -r PROD PROTO PLAT <<< "$cfg"
    BUILD_DIR="build/${PROD}_${PROTO}_${PLAT}"
    echo "=== Building ${PROD} + ${PROTO} on ${PLAT} ==="
    cmake -B "$BUILD_DIR" \
          -DPRODUCT="$PROD" \
          -DPROTOCOL="$PROTO" \
          -DPLATFORM="$PLAT"
    cmake --build "$BUILD_DIR" -j$(nproc)
done
```

---

## 十三、项目目录结构总览

```
joystick-firmware/
│
├── CMakeLists.txt
├── build_all.sh
│
├── drivers/                    # 硬件抽象接口（只有 .h，ISP 拆分后）
│   ├── drv_common.h            # 公共类型（drv_status_t, adc_channel_id_t 等）
│   ├── adc_reader.h            # 基础 ADC 读取接口
│   ├── adc_voltage_reader.h    # 电压读取接口
│   ├── adc_continuous.h        # DMA 连续采集接口
│   ├── drv_can.h
│   ├── drv_gpio.h              # 拆分为 gpio_reader_t / gpio_writer_t
│   ├── drv_dac.h
│   ├── drv_timer.h
│   ├── drv_spi.h               # SPI 接口（霍尔传感器等）
│   └── drv_nv.h
│
├── input/                      # 输入采集层（scan_buffer 解耦模式）
│   ├── scan_buffer.h           # 纯数据缓冲区（解耦核心，零外部依赖）
│   ├── buf_axis_source.h       # 基于 scan_buffer 的轴输入源
│   ├── buf_axis_source.c       # buf_axis_source 实现
│   ├── buf_btn_source.h        # 基于 scan_buffer 的按键输入源（阈值检测）
│   └── buf_btn_source.c        # buf_btn_source 实现
│
├── board/                      # 板级扫描策略（硬件拓扑相关）
│   ├── board_scan.h            # 扫描策略接口 (board_scanner_t)
│   ├── board_scan.c            # 策略工厂（根据产品配置创建策略）
│   ├── mux_adc_scan.c          # MUX + ADC 逐通道扫描
│   ├── direct_adc_scan.c       # 多独立 ADC 通道直接读取
│   └── dma_scan.c              # DMA 连续采集
│
├── platform/                   # 各平台的驱动实现（DIP：显式注册）
│   ├── platform_registry.h     # 平台注册表接口 + platform_init() 声明
│   ├── platform_registry.c     # 平台注册表实现
│   ├── stm32g4/
│   │   ├── cubemx/
│   │   ├── adc_stm32g4.c
│   │   ├── can_stm32g4.c
│   │   ├── gpio_stm32g4.c
│   │   ├── spi_stm32g4.c      # SPI 驱动（霍尔传感器通信）
│   │   ├── dac_stm32g4.c
│   │   └── platform_init.c    # 显式注册所有驱动（替代 __attribute__((constructor))）
│   ├── s32k144/
│   │   ├── adc_s32k.c
│   │   ├── ...
│   │   └── platform_init.c
│   └── test/                   # PC 模拟驱动
│       ├── adc_mock.c
│       ├── can_mock.c
│       ├── gpio_mock.c
│       └── platform_init.c
│
├── signal/                     # 信号处理管线（SRP 拆分）
│   ├── signal_chain.h/c        # 处理链框架
│   ├── fault_detector.h/c      # 开路/短路检测
│   ├── calibrator.h/c          # 线性校准
│   ├── filter.h/c              # 数字滤波（低通等）
│   ├── deadzone.h/c            # 死区处理（支持策略注入）
│   └── debounce.h/c            # 按键消抖
│
├── protocols/                  # 协议插件（LSP 能力模型）
│   ├── protocol_interface.h    # 核心接口 + 能力枚举 + 扩展接口
│   ├── protocol_registry.c/h
│   ├── canopen/
│   │   ├── canopen_plugin.c
│   │   ├── canopen_od.c
│   │   └── canopen_config.h
│   ├── j1939/
│   │   ├── j1939_plugin.c
│   │   └── j1939_pgn_map.h
│   ├── analog/
│   │   └── analog_plugin.c
│   └── can_raw/
│       └── can_raw_plugin.c
│
├── event/                      # 事件总线（观察者模式）
│   ├── event_bus.h             # 含 ISR 安全的 publish_from_isr()
│   └── event_bus.c
│
├── os/                         # RTOS 抽象层（新增）
│   ├── os_abstraction.h        # 统一 RTOS 接口
│   ├── os_freertos.c           # FreeRTOS 适配实现
│   ├── os_threadx.c            # ThreadX 适配实现
│   └── os_baremetal.c          # 裸机协作式调度（测试用）
│
├── app/                        # 应用核心
│   ├── app_compose.c/h         # Composition Root（依赖组装）
│   ├── app_tasks.c/h           # RTOS 任务创建与入口函数
│   ├── device_state.c/h        # 设备状态机（状态模式）
│   └── joystick_data.h         # 通用数据模型
│
├── service/                    # 通用服务
│   ├── calibration.c/h
│   ├── diagnostics.c/h
│   ├── nv_storage.c/h
│   └── logging.c/h
│
├── safety/                     # 功能安全（条件编译）
│   ├── safety_monitor.c/h
│   ├── stl_wrapper.c/h
│   ├── watchdog.c/h
│   └── error_handler.c/h
│
├── config/                     # 产品配置（OCP：每产品一文件）
│   ├── product_descriptor.h    # 产品描述符接口（永不修改）
│   └── products/
│       ├── js100.c             # JS-100 配置
│       ├── js200.c             # JS-200 配置
│       ├── js300.c             # JS-300 配置
│       └── js400.c             # JS-400 配置（新增产品只需新增文件）
│
└── test/                       # 单元测试（PC 上运行）
    ├── test_signal_chain.c
    ├── test_fault_detector.c
    ├── test_calibrator.c
    ├── test_deadzone.c
    ├── test_event_bus.c
    ├── test_device_state.c
    ├── test_axis_source.c      # 输入源抽象测试
    ├── test_canopen_mapping.c
    ├── test_j1939_packing.c
    └── test_safety_monitor.c
```

---

## 十四、完整依赖关系图

```
                         ┌──────────────┐
                         │  event_bus   │ ←── 解耦的通信骨架
                         └──────┬───────┘
                                │ subscribe/publish
              ┌─────────────────┼──────────────────┐
              ↓                 ↓                  ↓
     ┌──────────────┐  ┌───────────────┐  ┌─────────────────┐
     │ device_state │  │  app_tasks    │  │ safety_monitor   │
     │ (状态机)      │  │ (RTOS 任务)   │  │ (安全监控,最高优先级)│
     └──────┬───────┘  └──────┬────────┘  └────────┬────────┘
            │                 │                     │
            │     ┌───────────┼───────────┐        │
            ↓     ↓           ↓           ↓        ↓
       ┌─────────────┐ ┌────────────┐ ┌──────────────┐
       │ joystick    │ │  protocol  │ │  diagnostics │
       │ (信号处理链) │ │  (插件)    │ │  (STL/WDG)   │
       └──────┬──────┘ └─────┬──────┘ └──────┬───────┘
              │              │               │
              ↓              │               │
  ┌──────────────────────┐   │               │
  │  Input Acquisition   │   │               │
  │  scan_buffer_t       │   │               │
  │  buf_axis / buf_btn  │   │               │
  └──────────┬───────────┘   │               │
             │               │               │
             ↓               ↓               ↓
  ┌──────────────────────────────────────────────────┐
  │         drivers/*.h (接口定义)                     │ ←── 依赖倒置边界
  │   adc_reader / can_driver / gpio / spi / dac     │
  └──────────────────────┬───────────────────────────┘
                         ↑ 实现（依赖方向向上）
  ┌──────────────────────┴───────────────────────────┐
  │         platform_registry.h                      │
  │         platform_init() ← 显式调用（MISRA 合规）  │
  └──────────────────────┬───────────────────────────┘
                         ↑ 注册
              ┌──────────┼──────────┐
              │          │          │
       ┌──────┴───┐ ┌────┴─────┐ ┌──┴───────┐
       │ STM32G4  │ │ S32K144  │ │ PC Mock  │
       │ platform │ │ platform │ │ platform │
       └──────────┘ └──────────┘ └──────────┘
```

关键变化（相比 v2.0）：
- 新增 Input Acquisition 层，采用 scan_buffer 扫描-缓冲解耦模式
- 新增 board/ 层，板级扫描策略（mux_adc_scan / direct_adc_scan / dma_scan）
- 平台注册改为显式 `platform_init()` 调用，去除 GCC 特性
- app_core 拆分为 RTOS 多任务（safety/main/proto/diag）
- drivers 层新增 SPI 接口（霍尔传感器通信）

---

## 十五、设计原则对照总结

### SOLID 原则合规性

| 原则 | 原设计状态 | 改进方案 | 改进后状态 |
|------|-----------|---------|-----------|
| **S** — 单一职责 | `joystick_core.c` 五职责合一；`main_app.c` 混合组装与运行 | 信号处理管线拆分为独立阶段；组装与运行分离为 `app_compose` / `app_run` | 每个模块只有一个变更理由 |
| **O** — 开闭原则 | `product_config.h` 的 `#elif` 链，每加产品必须修改 | 每产品一个独立 `.c` 文件，CMake 链接选择 | 新增产品不修改任何已有文件 |
| **L** — 里氏替换 | 协议接口强制所有方法，模拟量输出需要写空函数 | 核心接口 + 能力位掩码 + 可选扩展方法 | 所有协议都可以安全替换使用 |
| **I** — 接口隔离 | `adc_driver_t` 包含所有方法，消费者被迫依赖不需要的功能 | 拆分为 `adc_reader` / `adc_voltage_reader` / `adc_continuous` | 每个模块只依赖自己需要的最小接口 |
| **D** — 依赖倒置 | `#if defined(PLATFORM_STM32G4)` 硬编码平台；协议插件内部驱动来源不明 | 平台注册表 + 显式依赖注入 | 上层完全不知道下层具体实现 |

### 新增设计模式

| 模式 | 用途 | 位置 |
|------|------|------|
| **观察者模式** | 模块间松耦合事件通信 | `event/event_bus.h` |
| **策略模式** | 可插拔的死区曲线算法 | `signal/deadzone.h` 中的 `deadzone_fn_t` |
| **状态模式** | 设备生命周期管理 | `app/device_state.h` |
| **管道模式** | 信号处理阶段串联 | `signal/signal_chain.h` |
| **扫描-缓冲模式** | 硬件扫描与数据消费解耦 | `input/scan_buffer.h` + `board/board_scan.h` |

### v3 相比 v2 的关键改进

| 改进点 | v2 设计 | v3 改进 | 原因 |
|--------|---------|---------|------|
| 平台注册 | `__attribute__((constructor))` 自动注册 | 显式 `platform_init()` 调用 | MISRA C 合规，功能安全认证要求 |
| 输入采集 | 假设"轴 = ADC 通道" | `scan_buffer_t` + `board_scanner_t` 扫描-缓冲解耦 | MUX 与轴/按键共用资源需解耦 |
| 按键采集 | 仅 GPIO 直读 | `buf_btn_source`（逐通道阈值检测，支持多键同按） | 实际硬件为 5V→2.5V 分压接 MUX |
| 归一化范围 | 固定 ±10000 | 内部无符号原始范围，协议输出层按需转换 | 12-bit ADC 用 ±10000 有虚假精度，且输入信号本身为正值 |
| 应用架构 | 裸 `for(;;)` 循环 | RTOS 多任务（safety/main/proto/diag） | 安全监控需要优先级保障 |
| 事件总线 | 仅任务上下文同步分发 | 增加 ISR 安全的延迟分发 | CAN RX 中断需要发事件 |
| 驱动接口 | 无 SPI | 新增 `drv_spi.h` | 霍尔角度传感器通信 |

### 关键约束

所有改进都保持了嵌入式 C 的实用性与功能安全合规性：
- 纯 C99 标准，无 GCC/编译器特有扩展
- 无虚函数表 / RTTI
- 无动态内存分配（`malloc`/`free`）
- 无 C++ 异常
- 所有缓冲区静态分配
- 事件总线订阅者数量编译时确定
- 所有初始化顺序显式可控（无 constructor / 隐式初始化）
- MISRA C:2012 可达标
